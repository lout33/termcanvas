# X / Twitter thread draft

> Build-in-public voice (matches the operating mode). Post from Luis's account. Attach the demo GIF/video to tweet 1 — it's the converter. Don't over-hashtag.

## Tweet 1 (hook + demo)
```
I run a bunch of AI coding agents at once and kept losing track of them in a pile
of terminal tabs.

So I built TermCanvas: an open-source canvas where every shell + agent is a node,
wired with commander→worker delegation lines.

macOS / Apple Silicon. 🧵
[attach demo GIF or YouTube link]
```

## Tweet 2 (what's different — honest)
```
There are other "canvas of agents" tools now (Maestri, OpenCove, Agent Grid), so
here's what's actually different about this one:

→ open source, local-first (no account, no credits)
→ tmux-backed: sessions survive crashes/reboots/SSH, reattach from any terminal
```

## Tweet 3 (the thesis)
```
The bet: the real bottleneck on multi-agent work isn't compute, it's *your*
steering attention.

A spatial map where agents have positions + visible delegation lines lets you
hold more of them in your head than a tab list or a kanban board.
```

## Tweet 4 (honest status + CTA)
```
Status, no hype: v0.2.5, solo-built, macOS Apple Silicon only (cross-platform is
the #1 ask).

It's open source — kick the tires, tell me where the delegation model breaks:
https://github.com/lout33/termcanvas
```

## Optional Tweet 5 (build-in-public)
```
I'm building this in the open and streaming the work. If "a cockpit for steering
a fleet of agents" is a thing you want, follow along — feedback shapes the
roadmap.
```

## Notes
- Lead image/video matters more than copy. Use the existing `docs/termcanvas-demo.gif`.
- If reach is low, quote-tweet relevant agent-orchestration discussions with a genuine take (NOT a drive-by "try mine" — that's the line we don't cross).
